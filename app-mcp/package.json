{
  "name": "app-mcp",
  "version": "1.0.0",
  "description": "MongoDB MCP server for the Murder Mystery app (Atlas, read-only)",
  "type": "commonjs",
  "scripts": {
    "start": "MDB_MCP_READ_ONLY=${MDB_MCP_READ_ONLY:-true} mongodb-mcp-server --transport http --httpHost=${MDB_MCP_HTTP_HOST:-0.0.0.0} --httpPort=${MDB_MCP_HTTP_PORT:-8080}",
    "dev": "npm run start"
  },
  "dependencies": {
    "mongodb-mcp-server": "^1.2.0"
  }
}
